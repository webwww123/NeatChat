"use client";

import { useEffect } from "react";
// import { useChatStore } from "../store/chat";

export function McpInitializer() {
  // 已在app/page.tsx中初始化，这里不再重复初始化
  // useEffect(() => {
  //   useChatStore.getState().initMcp();
  // }, []);

  return null; // 这个组件不渲染任何内容
}
